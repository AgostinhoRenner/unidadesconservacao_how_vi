# Conexão com MySQL (rode: docker network create dev-network && docker run -d --name mysql-dev --network dev-network -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=uc_db -p 3306:3306 --restart unless-stopped mysql:8)
DB_HOST=mysql-dev
DB_PORT=3306
DB_USER=root
DB_PASS=root
DB_NAME=uc_db
